#ifndef RECTANGLE_H
#define RECTANGLE_H

#include <string>
#include <vector>
using std::string; 
using std::vector; 

class Rectangle {
public:
  void displayInRectangle(const vector<string> description, const string around);
};

#endif