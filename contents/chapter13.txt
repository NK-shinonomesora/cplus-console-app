13.ムーブコンストラクタとムーブ代入

例えばswapのように2つのメモリ領域の値を入れ替える場合、
一方の値を一時的な領域にコピーしておき、不要になったら
その領域を解放する手段が考えられる。
但し、この実装では一時的な領域の確保などの無駄が生じる。
そこでコピーに代わりムーブを利用する。
ムーブを利用することにより、一時的なメモリ領域は確保せず、
2つのメモリ領域の所有権を入れ替えることによりswapを実現する。